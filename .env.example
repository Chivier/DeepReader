# DeepReader 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================================================
# OpenAI API 配置
# ============================================================================
# OpenAI API 密钥（必需）
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI API 基础URL（可选，用于自定义端点）
# 例如：https://api.openai.com/v1 或自定义代理地址
OPENAI_BASE_URL=https://api.openai.com/v1

# 默认使用的模型名称（可选）
# 可选值：gpt-4o, gpt-4o-mini, gpt-3.5-turbo 等
DEEPREADER_MODEL_NAME=gpt-4o

# ============================================================================
# Jina Reader 配置（可选）
# ============================================================================
# Jina Reader URL，用于网页内容提取
JINA_READER_URL=

# ============================================================================
# 其他配置
# ============================================================================
# Cairo 库路径（macOS 用户可能需要）
# DYLD_LIBRARY_PATH=/opt/homebrew/lib:$DYLD_LIBRARY_PATH

# ============================================================================
# 注意事项
# ============================================================================
# 1. 不要将 .env 文件提交到版本控制系统
# 2. 确保 .env 文件已添加到 .gitignore
# 3. API 密钥请妥善保管，避免泄露
# 4. 某些环境变量可能需要重启应用才能生效